开始程序

1. 获取 <canvas> 元素
2. 检查浏览器是否支持 WebGPU
   - 如果不支持 → 抛出错误并停止执行

3. 请求 GPU 适配器 (adapter)
   - 如果未获取到 → 抛出错误并停止执行

4. 从适配器请求 GPU 设备 (device)

5. 获取 canvas 的 WebGPU 上下文 (context)
6. 询问浏览器推荐的画布格式 (canvasFormat)
7. 配置上下文 (context.configure):
   - 指定使用的 device
   - 指定渲染格式 canvasFormat

8. 定义顶点数据 (vertices)
   - 六个顶点 → 构成两个三角形 → 拼成一个正方形

9. 创建顶点缓冲区 (vertexBuffer)
   - 用于存储上面的顶点数据
   - 标记用途为 VERTEX 和 COPY_DST

10. 将顶点数据写入 GPU 缓冲区 (writeBuffer)

11. 定义顶点缓冲布局 (vertexBufferLayout)
    - 每个顶点占 8 字节（2 个 float32）
    - 顶点位置属性绑定到 shader 的 location(0)

12. 创建着色器模块 (cellShaderModule)
    - 顶点着色器 (vertexMain):
        输入: 顶点坐标 pos (vec2)
        输出: 顶点位置 vec4(pos, 0, 1)
    - 片元着色器 (fragmentMain):
        输出: 固定颜色 vec4(1, 0, 0, 1)（红色）

13. 创建渲染管线 (cellPipeline)
    - 指定：
        - 着色器模块及入口函数
        - 顶点缓冲布局
        - 渲染目标格式 (canvasFormat)

14. 创建命令编码器 (encoder)
15. 开始渲染通道 (beginRenderPass)
    - 清空背景色为深蓝色 (r=0, g=0, b=0.4)
    - 设置渲染目标为当前帧的纹理视图

16. 在渲染通道中：
    - 绑定渲染管线 (setPipeline)
    - 绑定顶点缓冲区 (setVertexBuffer)
    - 发出绘制命令 draw(顶点数量 = vertices.length / 2)

17. 结束渲染通道 (endRenderPass)
18. 完成命令编码 (encoder.finish)
19. 提交命令缓冲到 GPU 队列 (device.queue.submit)

结束程序
